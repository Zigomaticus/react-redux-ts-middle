import { Row } from "antd";
import { Header } from "antd/lib/layout/layout";
import React, { FC } from "react";

const Navbar: FC = () => {
  return (
    <Header>
      <Row justify="end">sss</Row>
    </Header>
  );
};

export default Navbar;
