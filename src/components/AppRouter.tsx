// Libraries
import React, { FC } from "react";
import { Routes, Route } from "react-router-dom";

const AppRouter: FC = () => {
  const auth = true;

  return <div>auth</div>;
};

export default AppRouter;
